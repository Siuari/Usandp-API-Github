@{
    ViewData["Title"] = "Home Page";
}

<nav class="navbar-default col-lg-10">
    <div class="form-group">
        <h2 class="col-md-offset-4">Meus repositorios GitHub</h2>
        
            <btn class="btn btn-default col-md-offset-5" id="pesquisa-btn">PESQUISAR</btn>
            <btn class="btn btn-default" id="listar-btn">LISTAR</btn>
    
    </div>
</nav>

<div class="row">
    <div class="form-group">
        <table class="col-md-offset-0"style="width:100%" id="tabela">
            <tr>
                <th class="col-lg-4">Repositório</th>
                <th class="col-lg-4">Link GitHub</th>
                <th class="col-lg-4">Favorito</th>
            </tr>
        </table>
    </div>
</div>

<div class="modal fade" id="modal-mensagem">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal"><span>×</span></button>
                <h4 class="modal-title">Título da mensagem</h4>
            </div>
            <div class="modal-body">
                <p id="mensagem">Conteúdo da mensagem</p>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-default" data-dismiss="modal">Fechar</button>
            </div>
        </div>
    </div>
</div>


<script type="text/javascript" src="~/js/Index.js">

/*const pesquisa = document.getElementById("pesquisa-btn");

    pesquisa.addEventListener('click', function () {
        var texto = $("#pesquisa-text").val();
        $("#modal-mensagem").modal('show'); 
        $("#mensagem").html(texto);

    });

    /*
    const pesq = document.getElementById("pesquisa-btn");
    const url = "https://github.com/Siuari?tab=repositories"
    const token_git = "8ff37ff0a26a5555ef9270d3342ac1bcee3939a5"

    async function getRepo(repo) {
        const resposta = await fetch(url + "/" + repo + token_git);

        const profile = resposta.json();
        console.log(profile)
    }

    pesq.addEventListener('click', getRepo);
    async function getRepo() {
        const response = await fetch(url);
        const result = await response.json()

        console.log(result)
    }*/
</script>

